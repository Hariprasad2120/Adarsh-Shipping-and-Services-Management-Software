# Database (Neon Postgres for production, local for dev)
DATABASE_URL="postgresql://neondb_owner:npg_HYCs1Mj5GlBR@ep-weathered-wave-aooaz68o-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=verify-full&channel_binding=require"

# NextAuth
NEXTAUTH_SECRET="6b8fb1092a821a6b0b16c0ef7a9b1309797424e0d8386806aec55f8a829b9d0b"
NEXTAUTH_URL="http://localhost:3000"

# Email provider: "resend" (Vercel) or "smtp" (self-host)
EMAIL_PROVIDER="resend"
EMAIL_FROM="noreply@yourdomain.com"

# Resend (if EMAIL_PROVIDER=resend)
RESEND_API_KEY=""

# SMTP (if EMAIL_PROVIDER=smtp)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""

# Cron security token (must match x-cron-secret header)
CRON_SECRET="change-me-to-a-random-secret"

# ERPNext integration (Frappe bench on WSL)
# ERPNEXT_HOST: the site hostname Frappe expects (used as Host header + base for URLs)
# ERPNEXT_IP: actual TCP address (WSL IP or production hostname — omit in prod if DNS resolves)
ERPNEXT_HOST="erpnext.localhost"
ERPNEXT_PORT="8000"
ERPNEXT_IP="172.19.114.20"
ERPNEXT_API_KEY="11450bbfd132d18"
ERPNEXT_API_SECRET="9d045a21ee6f785"

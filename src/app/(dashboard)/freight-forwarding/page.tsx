import { FreightForwardingWorkspace } from "@/modules/freight-forwarding/components";

export const metadata = {
  title: "Freight Forwarding | Adarsh Shipping",
};

export default function FreightForwardingPage() {
  return <FreightForwardingWorkspace />;
}

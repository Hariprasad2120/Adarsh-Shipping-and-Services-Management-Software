package com.monolith.crm.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark Mode Colors ──
val DarkBackground = Color(0xFF0D1117)
val DarkSurface = Color(0xFF161B22)
val DarkSurfaceLow = Color(0xFF161B22)
val DarkSurfaceContainer = Color(0xFF21262D)

// ── Light Mode Colors ──
val LightBackground = Color(0xFFF7F9FB)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceLow = Color(0xFFF2F4F6)
val LightSurfaceContainer = Color(0xFFECEEF0)

// ── Accent Colors ──
val CyanPrimary = Color(0xFF00CEC4)
val CyanAccent = Color(0xFF00CEC4)
val OrangeAlert = Color(0xFFFB923C)

// ── Dark Mode Text / Outline ──
val OnSurface = Color(0xFFF0F6FC)
val OnSurfaceVariant = Color(0xFF8B949E)
val OutlineColor = Color(0xFF30363D)

// ── Light Mode Text / Outline ──
val LightOnSurface = Color(0xFF191C1E)
val LightOnSurfaceVariant = Color(0xFF404947)
val LightOutlineColor = Color(0xFF707977)
val LightOutlineVariant = Color(0xFFBFC8C6)

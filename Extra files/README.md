# Extra files

Preserved material that is **not** part of the production application but must be
kept for reference/history. Nothing here is imported, built, or deployed.

- Excluded from Vercel deploys via `.vercelignore`.
- Still tracked in git — **do not delete**.

## Layout

| Folder | Contents |
|---|---|
| `References/` | Engine documentation PDFs, costing proposal deck |
| `Legacy/` | Superseded copies kept for history: old `frappe-ui-design-system.{css,md}` (live copy is `frappe_docker/design/frappe-ui-design-system.css`), `AGENTS-v1.md` (superseded by root `AGENTS.md`), stray Windows SDK binary |
| `Documentation/` | Completed task specs and one-off analysis docs (`AMS_TASK.md`, `TASK.md`, `WORK_PET_*.md`, `VERCEL_MIGRATION_AUDIT.md`) |
| `Documentation/prompts/` | Historical build-prompt specifications (Claude / Codex master prompts) |

Moved here during the Production Preparation / Repository Cleanup phase
(see root `TAST.md`, STEP 7). 2026-08-29.
